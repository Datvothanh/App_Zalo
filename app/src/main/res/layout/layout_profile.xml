<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp"
    android:gravity="center"
    tools:context=".Fragments.ProfileFragment">

    <ImageView
        android:id="@+id/profile_img_avatar"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:layout_centerVertical="true" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_edt_first_name"
        android:hint="Tên"
        android:textColor="@color/profileText"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_edt_last_name"
        android:hint="Họ"
        android:textColor="@color/profileText"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_edt_phone"
        android:hint="SĐT"
        android:textColor="@color/profileText"/>


    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:layout_marginTop="30dp"
        android:id="@+id/profile_edt_full_bio"
        android:hint="Trạng thái"
        android:textColor="@color/profileText"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/btn_logout_profile"
        android:layout_marginTop="30dp"
        android:text="Đăng xuất"/>

</LinearLayout>
